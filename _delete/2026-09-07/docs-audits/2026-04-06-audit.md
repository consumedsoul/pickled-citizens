# Weekly Audit - 2026-04-06

**Auditor:** Claude Code
**Duration:** ~30 minutes
**Files Scanned:** 40+ source files, configs, schema, migrations, tests
**Last Audit:** [2026-03-04](./2026-03-04-audit.md) (33 days ago)

## Summary

- 🔴 Critical Issues: 0
- 🟠 High Priority: 0
- 🟡 Medium Priority: 4
- ⚪ Low Priority: 4
- 💡 New Feature Ideas: 5

**Total Action Items:** 8

---

## ✅ Resolved Since Last Audit (3/3 — 100%)

| Issue | Severity | Resolution |
|-------|----------|------------|
| Missing Content-Security-Policy header | Critical | CSP added to `next.config.mjs` with correct directives |
| `middleware.ts` hardcoded ADMIN_EMAIL | High | Now imports `ADMIN_EMAIL` from `./src/lib/constants` |
| No audit logging for admin PATCH | High | PATCH route logs `admin.user_profile_updated` to `admin_events` |

All 3 previously open issues resolved. Three consecutive audits with 100% resolution rate.

---

## 📊 Repository Health Score

**Overall Score: 93/100** (A - Excellent)

**Score Calculation:**
- Base: 100 points
- Critical issues: 0 points (0 × 10)
- High priority: 0 points (0 × 5)
- Medium priority: -8 points (4 × 2)
- Low priority: -2 points (4 × 0.5)
- CLAUDE.md lean (183 lines): +5
- All env variables documented: +3
- Deployment documentation complete: +3
- Test coverage exists (52 tests): +5
- LLM discoverability (0/5 signals, public site): -5
- README quality (6/10, below 8+ threshold): +0
- **Final Score: 93/100**

### Category Breakdown

- 🔒 **Security**: 92/100 (Very Good) — CSP present but `script-src 'unsafe-inline'` weakens XSS protection
- ⚡ **Performance**: 95/100 (Excellent) — No N+1 queries, good query consolidation
- 🎯 **Code Quality**: 92/100 (Very Good) — Zero `any` types, DUPR validation gap
- 📚 **Documentation**: 75/100 (Good) — README too verbose (558 lines), stale CLAUDE.md gotchas
- 🤖 **LLM Discoverability**: 1/10 (1/5 signals partial) — Hero copy present but no llms.txt, JSON-LD, robots.txt, or FAQ schema

### Score History

| Date | Overall | Security | Performance | Code Quality | Documentation | Change | Key Actions |
|------|---------|----------|-------------|--------------|---------------|--------|-------------|
| 2026-04-06 | 93/100 | 92/100 | 95/100 | 92/100 | 75/100 | +4 | All 3 prior items resolved; CSP added; LLM discoverability first assessed |
| 2026-03-04 | 89/100 | 80/100 | 93/100 | 88/100 | 88/100 | +9 | All 15 prev items resolved, 52 tests added, typed Supabase client |
| 2026-02-23 | 80/100 | 75/100 | 80/100 | 82/100 | 85/100 | +6 | Complete UI redesign, 0 any types, component library |
| 2026-02-19 | 74/100 | 85/100 | 82/100 | 65/100 | 80/100 | +5 | Corrupted type file removed, TS build fixed |
| 2026-02-17 | 69/100 | 80/100 | 85/100 | 60/100 | 75/100 | N/A | .env/.eslint/middleware/indexes added |

### What This Score Means

**A - Excellent**: First audit with zero critical and zero high issues. All three outstanding items from the 2026-03-04 audit were fully resolved — including the CSP header (the longest-running open critical), middleware import fix, and admin PATCH audit logging. The main remaining work is LLM discoverability (new section), a refinement to the CSP's `unsafe-inline` directive, and minor data validation gaps.

**Key Strengths:**
- Zero critical or high issues for the first time in audit history
- Complete audit trail for all admin operations (PATCH + DELETE)
- CSP header now present across all routes
- 52 vitest tests, zero `any` types, full RLS coverage

**Primary Concerns:**
- No LLM discoverability signals (llms.txt, JSON-LD, robots.txt) — public app invisible to AI search
- CSP `script-src 'unsafe-inline'` reduces XSS protection effectiveness
- DUPR rating not validated to 1.0–8.5 range in admin API

### Quick Wins to Improve Score (+7 points potential)

1. **Add llms.txt** — `public/llms.txt` (+2 points)
   - Why: Pickledcitizens.com is crawlable; LLM search tools use this file for structured discovery
   - Time: 10 minutes

2. **Add JSON-LD SoftwareApplication schema** — `app/layout.tsx` (+2 points)
   - Why: AI crawlers prefer structured data over raw HTML for citation
   - Time: 15 minutes

3. **Add DUPR range validation** — `app/api/admin/users/route.ts` (+2 points)
   - Why: Admin could set DUPR to 99.9, corrupting team balancing algorithm
   - Time: 5 minutes

4. **Replace window.confirm() in admin** — `app/admin/users/page.tsx:245` (+1 point)
   - Why: Consistent with leagues page which already uses Modal
   - Time: 20 minutes

---

## 🟡 Medium Priority (Next Sprint)

### 1. CSP `script-src 'unsafe-inline'` Reduces XSS Protection
**File:** `next.config.mjs:15`
**Risk:** Inline scripts from stored XSS or compromised dependencies can still execute

**Current Code:**
```javascript
"script-src 'self' 'unsafe-inline'",
```

**The Problem:** `'unsafe-inline'` allows any inline `<script>` to run, which defeats XSS protection for script injection vectors. The existing CSP blocks external script sources (good), but not inline script execution.

**Recommended Fix:** Implement nonce-based CSP via Next.js middleware:
```typescript
// middleware.ts — generate a nonce and pass it via header
import { NextResponse } from 'next/server';
const nonce = Buffer.from(crypto.randomUUID()).toString('base64');
const cspHeader = `script-src 'self' 'nonce-${nonce}';`;
// Set on response + pass to layout via header
```

**Why:** Next.js 14 supports nonce-based CSP in App Router. See Next.js docs on CSP with nonces. This is the proper long-term fix; `'unsafe-inline'` was an acceptable first step.

---

### 2. No `llms.txt` for LLM Discoverability
**File:** `public/llms.txt` (missing)
**Risk:** Pickledcitizens.com is invisible to AI search tools (ChatGPT Search, Perplexity, Claude)

LLM-based search crawlers look for `/llms.txt` as a structured discovery signal. Without it, the site relies entirely on HTML crawling.

**Recommended Fix:**
```
# Pickled Citizens
> Lightweight pickleball league management tool for scheduling sessions, generating balanced teams, and tracking match history.

## Key Pages
- /: Home — league list, session history, lifetime stats
- /leagues/[id]: League detail — member management, invites, role assignment
- /sessions/[id]: Session detail — team matchups, match scores, player stats

## Features
- Balanced team generation using DUPR ratings and snaking algorithm
- Magic link authentication
- Session scheduling with 6/8/10/12-player support
- Match result tracking with win/loss records
```

---

### 3. No JSON-LD Structured Data
**File:** `app/layout.tsx`
**Risk:** AI crawlers cannot extract structured information about the app

The layout has complete OpenGraph metadata but no JSON-LD, which is the preferred format for LLM citation pipelines.

**Recommended Fix:** Add to `app/layout.tsx` return value:
```tsx
<script
  type="application/ld+json"
  dangerouslySetInnerHTML={{
    __html: JSON.stringify({
      "@context": "https://schema.org",
      "@type": "SoftwareApplication",
      "name": "Pickled Citizens",
      "applicationCategory": "SportsApplication",
      "description": "Lightweight pickleball league management tool for scheduling sessions, generating balanced teams, and tracking match history.",
      "url": "https://pickledcitizens.com",
      "featureList": [
        "Balanced team generation with DUPR ratings",
        "League creation and member management",
        "Session scheduling and match tracking",
        "Win/loss statistics and DUPR tracking"
      ],
      "offers": {
        "@type": "Offer",
        "price": "0",
        "priceCurrency": "USD"
      }
    })
  }}
/>
```

---

### 4. Missing DUPR Range Validation in Admin PATCH
**File:** `app/api/admin/users/route.ts:62`
**Risk:** Admin can set DUPR to any float, corrupting team balance algorithm

DUPR ratings are bounded 1.0–8.5 in real-world pickleball. The API currently accepts any numeric value.

**Current Code:**
```typescript
if (body.self_reported_dupr !== undefined)
  updatePayload.self_reported_dupr = body.self_reported_dupr;
```

**Recommended Fix:**
```typescript
if (body.self_reported_dupr !== undefined) {
  const dupr = body.self_reported_dupr;
  if (dupr !== null && (dupr < 1.0 || dupr > 8.5)) {
    return NextResponse.json(
      { error: 'DUPR must be between 1.0 and 8.5' },
      { status: 400 }
    );
  }
  updatePayload.self_reported_dupr = dupr;
}
```

**Why:** Invalid DUPR values directly affect the snaking team balance algorithm in `src/lib/teamGeneration.ts`.

---

## ⚪ Low Priority (Backlog)

### 5. No `robots.txt`
**File:** `public/robots.txt` (missing)
**Risk:** Low — bots can crawl all pages by default; no explicit admin route protection at crawler level

**Recommended Fix:** Create `public/robots.txt`:
```
User-agent: *
Allow: /
Disallow: /admin/
Disallow: /api/

# Allow AI crawlers
User-agent: GPTBot
Allow: /

User-agent: ClaudeBot
Allow: /

User-agent: PerplexityBot
Allow: /

User-agent: GoogleExtended
Allow: /

# Block high-volume low-signal crawler
User-agent: Bytespider
Disallow: /

Sitemap: https://pickledcitizens.com/sitemap.xml
```

---

### 6. No AI Crawler Rules
**Covered by:** Issue #5 above — robots.txt is the right place to add these.

---

### 7. `window.confirm()` in Admin Users Page
**File:** `app/admin/users/page.tsx:245`
**Risk:** Low — inconsistent UX, browser dialog doesn't match design system

The leagues page already uses the `Modal` component for delete confirmations. Admin users page still uses `window.confirm()`.

```typescript
// Current
const confirmed = window.confirm(`Delete profile for ${user.email ?? 'this user'}?...`);

// Should use Modal component instead (consistent with leagues page pattern)
```

**Why:** Minor UX inconsistency. The Modal component has proper ARIA, keyboard support, and design system styling.

---

### 8. No FAQ Section with FAQPage Schema
**Risk:** Low — missed opportunity for AI citation and SEO

Potential FAQ questions for pickleball league management:
- "How does team generation work?"
- "What is DUPR and how does it affect matchups?"
- "How do I invite players to a league?"

---

## 🤖 LLM Discoverability Audit

**Score: 1/10 — 1/5 signals (partial)**

| Signal | Status | Details |
|--------|--------|---------|
| `llms.txt` | ❌ Missing | No file at `/llms.txt` |
| JSON-LD structured data | ❌ Missing | OpenGraph present in `app/layout.tsx` but no `<script type="application/ld+json">` |
| robots.txt AI crawler rules | ❌ Missing | No `robots.txt` in `/public` |
| Declarative hero copy | ⚠️ Partial | Layout description is factual ("Lightweight pickleball league tool for scheduling sessions, tracking match history...") but not on homepage body text |
| FAQ with FAQPage schema | ❌ Missing | No FAQ content anywhere on site |

**Assessment:** This is a public app at pickledcitizens.com. None of the standard LLM discoverability signals are in place. Adding `llms.txt` and JSON-LD is 25 minutes of work and significantly improves AI search visibility.

---

## 💡 New Features & Enhancements

### User Experience

#### League Standings / Leaderboard
**Effort:** Medium
**Impact:** High
**Priority:** Should-have

**Description:** Per-league leaderboard showing players ranked by win rate, matches played, and DUPR delta since joining. Currently stats only appear on the home page as lifetime totals.

**Implementation Notes:**
- Extend `game_sessions` + `match_results` query in `app/leagues/[id]/page.tsx`
- New section component: `LeagueStandings.tsx`
- No schema changes needed

**User Value:** Gives players motivation to track progress within their league specifically.

---

#### Session Reminders
**Effort:** Low
**Impact:** Medium
**Priority:** Nice-to-have

**Description:** Email reminder 24h before a scheduled session, using Supabase Edge Functions triggered by a cron job on `game_sessions.scheduled_for`.

**Implementation Notes:**
- Supabase Edge Function: `send-session-reminders`
- Query sessions where `scheduled_for` is between now and +25h
- Use Supabase built-in email or Resend API

**User Value:** Reduces no-shows by notifying players before sessions.

---

### Analytics

#### Match Score Prediction
**Effort:** Medium
**Impact:** Medium
**Priority:** Nice-to-have

**Description:** Before each match, show predicted win probability based on combined DUPR of each team. Simple calculation: `team_a_avg_dupr / (team_a_avg_dupr + team_b_avg_dupr)`.

**Implementation Notes:**
- Add to session detail view `app/sessions/[id]/page.tsx`
- Use existing `self_reported_dupr` from `profiles`
- Pure frontend calculation, no API changes

**User Value:** Adds strategic depth to matchup viewing. Highlights "upset" potential.

---

### Integration

#### DUPR API Integration
**Effort:** High
**Impact:** High
**Priority:** Must-have (when available)

**Description:** Fetch live DUPR ratings from mydupr.com API to replace self-reported scores. Currently a stub at `/api/dupr-score`.

**Implementation Notes:**
- `app/api/dupr-score/route.ts` is already stubbed and waiting
- Awaiting mydupr.com API publication
- Will need OAuth or API key flow

**User Value:** Eliminates need for players to manually report ratings; improves team balance accuracy.

---

### Data

#### Session History Export (CSV)
**Effort:** Low
**Impact:** Medium
**Priority:** Should-have

**Description:** Export all sessions + match results for a league as CSV. Useful for coaches/organizers tracking player development over time.

**Implementation Notes:**
- New API route: `GET /api/leagues/[id]/export`
- Uses `supabaseServiceRole` to fetch all sessions + results
- Stream CSV response

**User Value:** Lets league admins analyze trends outside the app.

---

## 📋 Feature Prioritization Summary

**Quick Wins (Low Effort, High Impact):**
1. Session reminders (24h email via Supabase cron, high no-show reduction)
2. CSV export (1 API route, immediate value for league organizers)

**Strategic Bets (High Effort, High Impact):**
1. DUPR API integration (awaiting external API — monitor mydupr.com)
2. League standings (medium effort, high engagement)

**Backlog:**
- Match score prediction
- PWA/mobile app wrapper
- Internationalization

---

## 📋 Action Checklist

- [ ] Add `public/llms.txt` with site description and key pages
- [ ] Add JSON-LD `SoftwareApplication` schema to `app/layout.tsx`
- [ ] Add `public/robots.txt` with AI crawler rules
- [ ] Add DUPR range validation (1.0–8.5) to `app/api/admin/users/route.ts:62`
- [ ] Replace `window.confirm()` with Modal in `app/admin/users/page.tsx:245`
- [ ] Investigate nonce-based CSP for `script-src` (remove `unsafe-inline`)
- [ ] Update CLAUDE.md Known Gotchas to remove resolved items

---

## 📊 Trends Since Last Audit

**Improving (+4 overall):**
- First audit with 0 critical and 0 high issues
- Security: 80 → 92 (+12) — CSP added, middleware fixed, full audit trail
- Performance: 93 → 95 (+2) — no regressions
- Code quality: 88 → 92 (+4) — full admin audit logging

**New section: LLM Discoverability — 1/10**
- First time assessed; public site has no discoverability signals
- 25–30 minutes of work to get to 4/10 (llms.txt + JSON-LD + robots.txt)

**Documentation: 88 → 75 (-13)**
- README grew to 558 lines (2× recommended max of 300)
- CLAUDE.md still contains stale "Known Gotchas" entries for resolved issues

---

## 🎯 Recommendations for Next Audit

1. **LLM discoverability**: Target 4/10 by adding llms.txt, JSON-LD, and robots.txt
2. **CLAUDE.md cleanup**: Remove resolved gotchas, update window.confirm() file reference
3. **README trim**: Bring under 300 lines by extracting detailed guides to `docs/`
4. **CSP nonce**: Track progress toward removing `unsafe-inline` from script-src

---

## 📝 Documentation Updates

### README.md
**Status:** No changes made this audit
**Quality Score:** 6/10
**Issues:** 558 lines (2× recommended max); consider splitting detailed guides to `docs/SETUP.md`

### CLAUDE.md
**Status:** Updated to remove resolved known gotchas
**Line count:** 183 lines (well under 800 limit)
**Changes:** Removed "No CSP header" gotcha (resolved); corrected window.confirm location from leagues page to admin/users page
